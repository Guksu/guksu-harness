<!--
공통 배포 전 점검 템플릿 — fe-predeploy 스킬이 프론트엔드 배포 전 점검 결과를 이 형식으로 기록한다.
- {중괄호} 자리를 채우고 이 안내 주석은 삭제한다.
- 섹션 구조(1~5)는 추가·삭제·개명하지 않는다.
- 판정은 pass / fail / skip 3분류 — skip은 사유 필수, 근거는 수치(요청 수·잔존 수·측정값)로 적는다.
- 재점검 시 새 파일을 만들지 않고 이 문서의 판정을 갱신하고 "5. 재실행 이력"에 추가한다.
- 시크릿(토큰·키·비밀번호)은 값을 기록하지 않는다 — 발견 위치만 적는다.
- 종합 판정의 첫 단어가 결론이다(배포 가능/불가). fail 항목은 계측 이름만 적지 말고 "사용자에게 어떤 일이
  생기는가"를 한 줄로 풀어 쓴다 — 코드를 모르는 독자도 소비할 수 있어야 한다(절대 규칙 7).
-->

# 배포 전 점검: {대상 이름}

| 항목 | 내용 |
|------|------|
| 날짜 | {YYYY-MM-DD} |
| 대상 | {브랜치·커밋 또는 빌드 식별자} |
| 환경 | {URL · 프로덕션 빌드 여부 (dev 서버였다면 명시)} |
| 프레임워크 | {퓨어 JS / React / Next.js} |
| 종합 판정 | {배포 가능 / 배포 불가 (blocker fail {N}건) / 부분 판정 (사유)} |

## 1. 점검 범위

{확정한 핵심 플로우 목록 (사용자 확인). 도구 가용성 — 브라우저/lighthouse/e2e 유무와 그로 인한 skip 범위.}

## 2. 정적 게이트 (Phase 1)

| 검사 | 판정 | 근거 |
|------|------|------|
| 빌드 | {pass/fail} | {명령·종료 코드} |
| 타입체크 | {pass/fail/skip} | |
| 린트 | {pass/fail/skip} | |
| 테스트·e2e | {pass/fail/skip} | {통과/전체 수} |
| 정적 스캔 | {pass/fail} | {blocker {N}건 / warn {N}건 — 상세는 4절} |

## 3. 런타임·성능·품질 (Phase 2~3)

<!-- 플로우×항목으로 행을 늘린다. 심각도: blocker(배포 차단) / warn(검토 권고) -->

| 카테고리 | 항목 | 심각도 | 판정 | 근거 수치 |
|---------|------|--------|------|----------|
| 인터랙션 | {버튼 연타 → API 1회 ({플로우})} | blocker | {pass/fail/skip} | {5연타 → POST {N}건} |
| 메모리 | {마운트 {N}사이클 리스너 순증가} | blocker | | {+{N}} |
| 네트워크 | {실패 응답 에러 UI} | blocker | | |
| 성능 | {LCP / CLS / INP} | blocker | | {측정값 / 기준치} |
| 품질 | {콘솔 에러} | blocker | | {건수} |
| 배포 설정 | {번들 시크릿 / debugger} | blocker | | |

## 4. 사용자 검토 필요

{기계 판정이 불가한 항목(레이아웃 이상 스크린샷, 소스맵 정책, warn fail 목록)과 정적 스캔 warn 후보. 각 항목에 근거(스크린샷 경로·파일:라인)와 권고를 병기. 없으면 "없음".}

## 5. 재실행 이력

| 날짜 | 범위 | 결과 변화 |
|------|------|----------|
| {YYYY-MM-DD} | {초기 점검 / 실패 항목 재검} | {fail {N}→{N}} |
