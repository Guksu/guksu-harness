---
name: pr
description: "커밋·푸시·PR — 사용자가 요청한 git 업로드 작업만 수행한다. '커밋해줘/푸시해줘/PR 만들어줘', PR 본문 수정·베이스 변경 요청에 사용한다. 기존 브랜치 전략과 설정을 따르며 완료만으로 업로드를 시작하지 않는다."
---

# 커밋·푸시·PR

사용자가 요청한 범위까지 수행한다. 커밋만 요청하면 커밋까지, 푸시까지 요청하면 푸시까지, PR을 요청하면 PR까지 진행한다. 머지는 별도 요청 없이는 하지 않는다.

## 프로젝트 정책

- 작업 브랜치와 PR 기준은 기존 프로젝트 규칙을 따른다. `dev`나 `feat/`를 강제하지 않는다. 기준이 명확하지 않으면 원격 기본 브랜치를 확인하고 선택 근거를 알린다.
- 커밋 메시지는 기존 언어와 Conventional Commits 관례를 따른다. 작성자 표기는 프로젝트 정책을 따른다. `blockGitMutation.config.json`의 `blockAttribution: true`인 경우 Claude 표기 패턴이 차단된다(기본 false).
- 훅이 설치돼 있으면 `allowCommitPush` 설정을 따른다. 한 번의 커밋 요청을 영구 권한 변경으로 해석하지 않는다. 영구 설정 변경까지 이미 승인됐다면 다시 묻지 않는다.
- `requireHistoryDoc`은 프로젝트의 기록 정책이다. 활성화된 프로젝트에서는 기록 없이 push하지 않는다. 이번 작업의 기록 하나를 갱신하고 커밋에 포함한다.
- 파일은 경로를 명시해 스테이징한다. 무관한 변경을 함께 커밋하지 않는다.

## 실행

1. 사용자 요청 범위·현재 브랜치·미커밋 변경을 확인한다. 필요하면 `branch` 스킬로 작업 브랜치를 준비한다.
2. 의미 있는 변경, 커밋·PR 단위의 최종 기록은 `history` 스킬로 작성한다. 작은 변경마다 별도 문서를 늘리지 않는다.
3. 관련 검사와 `git diff --check`를 실행하고 변경을 검토한다. 실패·미검증 항목은 기록에 명시한다.
4. `git add <경로>`와 `git commit -m <메시지>`로 커밋한다. 훅이 지원하지 않는 간접 메시지 플래그나 이력 재작성 플래그를 우회하지 않는다.
5. 푸시가 요청됐으면 `git push -u origin <작업브랜치>`를 실행하고 원격 결과를 확인한다. 일시적 네트워크 오류만 제한적으로 재시도한다. 인증·권한·충돌 오류를 무한 재시도하지 않는다.
6. PR이 요청됐으면 제목·본문에 변경 결과와 검증을 쓰고 생성한다. 여러 줄 본문은 구조화된 도구 인자 또는 `--body-file`로 전달한다.
7. 커밋 해시와 푸시 결과, 생성했다면 PR 링크를 보고한다.

force push·reset·rebase·원격 브랜치 삭제는 이 흐름에 포함하지 않는다. 훅은 명령 패턴의 실수 방지 장치이며 사용자 승인을 판독하거나 모든 셸 우회를 차단하는 보안 경계가 아니다.
