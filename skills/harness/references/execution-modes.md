# 실행 모드 — 단일 우선 · Workflow · 에이전트 팀 · 서브 에이전트 · 루프

하네스의 실행 모드를 결정하는 기준과 모드별 사용법. **모드는 수평 선택지가 아니라 에스컬레이션 사다리다** — 가장 싼 모드에서 시작해, 그것으로 안 되는 이유를 답할 수 있을 때만 위로 올라간다.

## 목차
1. [에스컬레이션 사다리 — 단일 우선](#1-에스컬레이션-사다리--단일-우선)
2. [결정 트리](#2-결정-트리)
3. [Workflow — 결정적 오케스트레이션](#3-workflow--결정적-오케스트레이션)
4. [에이전트 팀](#4-에이전트-팀)
5. [서브 에이전트](#5-서브-에이전트)
6. [루프 — 검증자 주도 반복](#6-루프--검증자-주도-반복)
7. [대량 마이그레이션 — 재시도 루프 파이프라인](#7-대량-마이그레이션--재시도-루프-파이프라인)
8. [하이브리드](#8-하이브리드)
9. [아키텍처 패턴 → 모드 매핑](#9-아키텍처-패턴--모드-매핑)

## 1. 에스컬레이션 사다리 — 단일 우선

**멀티에이전트를 기본값으로 고르지 않는다.** 근거(측정치):

- 에이전트는 채팅 대비 ~4배, **멀티에이전트 시스템은 ~15배** 토큰을 쓴다. 성능 이득의 80%는 협업의 마법이 아니라 **토큰을 더 쓴 것 자체**로 설명된다(Anthropic, BrowseComp 분석). 즉 멀티에이전트의 본질은 "컨텍스트 창 용량을 병렬로 늘리는 것"이다 — 병렬화할 게 없으면 15배를 태우고 이득이 없다.
- 그래서 멀티에이전트가 유리한 건 **breadth-first**(독립된 여러 방향을 동시에 탐색) 작업이다. 공유 컨텍스트·에이전트 간 의존이 많은 도메인 — **대부분의 코딩 작업 포함** — 은 현재 부적합하다(Anthropic). 코딩은 병렬 가능한 조각이 적고, LLM이 실시간 위임·조율에 아직 약하다.
- OpenAI 공식 가이드도 같다: **단일 에이전트의 능력을 소진한 뒤에만** 멀티에이전트를 도입하라. 에이전트를 늘리는 것은 복잡도·오버헤드를 늘리는 일이다.

사다리 (위로 갈수록 비싸다 — 한 칸 올라갈 근거를 요구한다):

```
0. 직접 실행 (오케스트레이션 없음)         — 현재 세션이 직접. 대부분의 작업.
1. 서브 에이전트 1개                        — 컨텍스트 격리·단일 전문성이 필요할 때만
2. 루프 (단일 실행 + 검증자)                — 종료를 기계 검증하며 반복 수렴할 때
3. Workflow (결정적 다중 에이전트)          — 흐름을 코드로 쓸 수 있고 항목이 많을 때
4. 에이전트 팀 (협상형 다중 에이전트)       — 진행 중 협상·재작업이 본질일 때만 (최후)
```

**한 칸 올라가는 트리거 (OpenAI, 기계적 판정):** 단일 에이전트가 막힐 때 나타나는 두 신호로 판단한다. 없으면 올라가지 않는다.
1. **로직 과다** — 프롬프트가 if-then-else 분기로 뒤덮여 하나의 정책 템플릿으로 유지하기 어렵다.
2. **도구 과부하** — 개수가 아니라 **유사도**가 기준이다. 서로 겹치는 도구가 10개 미만이어도 실패하는 반면, 뚜렷이 구분되는 도구는 15개 이상도 처리된다. 따라서 분리 전에 **도구 이름·파라미터·설명을 먼저 개선**하고, 그래도 안 될 때만 에이전트를 나눈다.

## 2. 결정 트리

§1의 사다리를 판별 질문으로 편 것이다. 위에서부터 내려오며, 각 분기는 "더 싼 모드로 안 되는 이유"가 있을 때만 통과한다.

```
작업이 반복·수렴형인가? (같은 실행을 "될 때까지" 반복 — 테스트 통과까지 수정, 감시-대응)
├─ 예 → 종료를 기계적으로 검증할 수 있는가?
│   ├─ 예 → 루프 (§6 — loop 스킬로 4요소를 사용자와 확정. 한 바퀴 내부는 아래 모드와 조합)
│   │        ※ 대량 항목 × 파일 단위 재시도 수렴이면 → 대량 마이그레이션 패턴 (§7)
│   └─ 아니오 → 루프 금지. 검증 가능한 목표로 재정의하거나 1회성 작업으로 전환
└─ 아니오 → 단일 에이전트(직접 실행 또는 서브 1개)로 안 되는 이유가 있는가?
    │        (§1 트리거: 로직 과다 / 도구 유사도 과부하 — 없으면 여기서 멈춘다)
    ├─ 아니오 → 직접 실행 또는 서브 에이전트 1개 (§5, 필요 시 run_in_background)
    └─ 예 → 제어 흐름을 미리 코드로 쓸 수 있는가?
        ├─ 예 (팬아웃/파이프라인/반복, 항목 목록이 결정 가능)
        │   → Workflow (§3)  ※ 항목 수십~수백 개, 스키마 검증 산출물, 토큰 버짓 관리에 특히 유리
        └─ 아니오 (진행 중 피드백 루프·계약 협상·재작업이 본질)
            → 에이전트 팀 (§4, 최후 수단)  ※ 기획↔구현↔QA 같은 생성-검증 루프가 대표 사례
```

주의: "여러 에이전트가 협업한다"는 사실만으로 에이전트 팀을 고르지 않는다. 협업의 형태가 **사전에 결정 가능한 데이터 흐름**이면 Workflow가 더 싸고 빠르고 재현 가능하다. 팀은 통신 오버헤드가 실제 가치(토론·계약 조정)를 만들 때만 선택한다 — 순차 의존 작업을 팀으로 짜면 토큰만 15배 쓰고 이득이 없다(§1).

**다중 에이전트의 두 패턴 (OpenAI — 팀/Workflow 선택 시 골격 결정):**
- **매니저(manager)** — 중앙 LLM이 전문 에이전트를 도구 호출처럼 부르고 결과를 종합한다. 워크플로우 제어·사용자 상호작용을 한 곳이 쥔다. → 오케스트레이터가 통제·종합을 유지해야 하면 이 패턴(서브 에이전트/Workflow 모드에 대응).
- **분산(decentralized)** — 동료 에이전트들이 실행과 대화 상태를 넘기는 일방향 핸드오프. → 통제가 완전히 이전돼야 하면 이 패턴(에이전트 팀 모드에 대응). 핸드오프는 자유 서술 대화가 아니라 **명시적 상태 키**(산출물 경로+형식)로 넘긴다(§9, orchestrator-template 데이터 흐름).

## 3. Workflow — 결정적 오케스트레이션

`Workflow` 도구는 자바스크립트 스크립트로 서브 에이전트들을 결정적으로 조율한다. 모델이 "다음에 누굴 부를까"를 추론하지 않으므로 재현 가능하고, 수십~수백 항목 팬아웃에서 누락이 없다.

**언제 선택하는가:**
- 항목 목록 × 동일 처리: 파일 50개 마이그레이션, 챕터 20개 검수, 소스 30건 리서치
- 다단계 파이프라인: 찾기 → 검증 → 종합 (단계 간 의존이 데이터로 표현됨)
- 산출물 스키마 강제: `schema` 옵션으로 JSON Schema 검증된 구조화 출력을 받아야 할 때
- 적대적 검증: 발견 1건당 반박 에이전트 N명 투표 같은 품질 패턴

**핵심 API (스크립트 안에서 사용):**

| 함수 | 역할 |
|------|------|
| `agent(prompt, opts)` | 서브 에이전트 1개 스폰. `opts.schema`(JSON Schema)로 구조화 출력 강제, `opts.agentType`으로 `.claude/agents/`의 커스텀 에이전트 사용, `opts.isolation: 'worktree'`로 병렬 파일 수정 격리 |
| `pipeline(items, ...stages)` | 항목별로 단계를 독립 진행(단계 간 배리어 없음). **다단계 작업의 기본값** |
| `parallel(thunks)` | 전부 끝날 때까지 대기(배리어). 전체 결과가 동시에 필요할 때만(중복 제거, 0건 조기 종료) |
| `phase(title)` / `log(msg)` | 진행 상황 그룹핑/표시 |
| `budget` | 토큰 버짓 조회 — 버짓 기반 반복 깊이 조절 |
| `workflow(nameOrRef, args)` | 저장된 워크플로우 또는 스크립트 파일을 하위 단계로 실행 (1단계 중첩만 허용) |

**스크립트 골격 예시 (찾기→검증 파이프라인):**

```js
export const meta = {
  name: 'review-chapters',
  description: '챕터별 검수 후 발견사항 적대적 검증',
  phases: [{ title: '검수' }, { title: '검증' }],
}
const results = await pipeline(
  args.chapters,
  (ch) => agent(`${ch} 검수...`, { phase: '검수', schema: FINDINGS_SCHEMA }),
  (review) => parallel(review.findings.map((f) => () =>
    agent(`반박 시도: ${f.title}`, { phase: '검증', schema: VERDICT_SCHEMA })
      .then((v) => ({ ...f, verdict: v })))),
)
return results.flat().filter(Boolean).filter((f) => f.verdict?.isReal)
```

**재사용과 재개 — 하네스가 활용할 핵심 기능:**
- **저장 워크플로우**: 반복 실행할 스크립트는 `프로젝트/.claude/workflows/{name}.mjs`에 저장하고 `Workflow({name})` 또는 스크립트 안에서 `workflow(name, args)`로 호출한다. 오케스트레이터 스킬 본문에 골격 텍스트로만 두는 것보다 재사용·버전 관리·`args` 파라미터화가 깔끔하다 — Workflow 모드 하네스의 산출물로 남긴다.
- **`scriptPath` 반복 수정**: 모든 실행은 스크립트를 세션 디렉토리에 파일로 남긴다. 수정 재실행 시 스크립트 전문을 재전송하지 말고 그 파일을 Edit 후 `Workflow({scriptPath})`로 재호출한다.
- **`resumeFromRunId` 재개**: 이전 실행의 완료된 `agent()` 호출은 캐시로 즉시 반환되고, 수정·추가된 호출부터만 실제 실행된다. 오케스트레이터의 **부분 재실행**(Phase 0 컨텍스트 확인)을 Workflow 모드에서 구현하는 기본 수단이다.
- **`opts.effort`**: 모델 정책과 같은 원칙 — 기본은 세션 상속(생략). 기계적 단계만 `low`로 낮추고, 최난도 검증·심판 단계만 상향하며 이유를 주석으로 남긴다.

**주의사항:**
- 스크립트는 순수 JS(TS 문법 불가), `Date.now()`/`Math.random()` 사용 불가(재개 기능이 깨짐) — 타임스탬프는 `args`로 주입
- `meta`는 순수 리터럴이어야 한다
- 모델 오버라이드(`opts.model`)는 기본 생략 — 세션 모델 상속이 원칙
- Workflow는 사용자가 멀티 에이전트 오케스트레이션을 명시적으로 요청한 맥락에서 실행된다. 하네스의 오케스트레이터 스킬이 Workflow를 실행 수단으로 쓰는 경우, 그 사실을 스킬 description과 본문에 명시해 사용자가 스킬 호출로 이미 옵트인했음을 분명히 한다
- 에이전트 정의 파일(`.claude/agents/`)은 Workflow 모드에서도 만든다 — `agentType` 옵션으로 연결
- 결과가 비었거나 이상하면 추측하지 말고 transcript 디렉토리의 `journal.jsonl`을 읽는다 — 각 `agent()`의 실제 반환값이 기록되어 있다

## 4. 에이전트 팀

세션에는 암시적 팀이 항상 하나 존재한다 — 별도의 팀 생성·해체 단계가 없다. `Agent` 도구에 `name`을 지정해 팀원을 스폰하고, 이후 `SendMessage(to: 이름)`으로 그 팀원의 컨텍스트를 유지한 채 대화를 이어간다. 의존성 있는 작업은 `TaskCreate`로 공유 작업 목록에 등록하면 팀원들이 상태를 갱신하며 자체 조율한다. 리더(오케스트레이터)는 진행을 모니터링하고 결과를 종합한다.

> **구 API 주의:** `TeamCreate`/`TeamDelete`는 더 이상 존재하지 않고, `Agent` 도구의 `team_name` 파라미터는 무시된다(deprecated). 감사(Phase 0)에서 기존 하네스가 이 도구들을 참조하면 현행화 대상이다.

**언제 선택하는가:**
- 생성-검증 피드백 루프: developer가 모듈을 완성할 때마다 QA가 교차검증하고 수정 요청을 보내는 구조
- 계약 협상: planner의 계획이 모호하면 developer가 질문하고 계획이 갱신되는 구조
- 중간 산출물 상호 참조: 서로의 진행 중 결과를 보고 방향을 조정해야 할 때

**골격:**

```
Agent(name: "planner", subagent_type, prompt: 정의 전문 + 작업 컨텍스트 + 산출물 경로)
Agent(name: "developer", ...)   ← 독립 팀원은 한 메시지에 병렬 스폰
TaskCreate(tasks: [{ title, assignee, depends_on }...])
→ 팀원 자체 조율 (SendMessage) — 리더는 TaskList/TaskGet으로 모니터링, 막힌 팀원은 SendMessage로 지원
→ 산출물 파일 확인 → 종료 판정 → 사용자 보고
```

**팀 크기:** 3명의 집중된 팀원이 5명의 산만한 팀원보다 낫다.

| 작업 규모 | 권장 팀원 수 |
|----------|------------|
| 소규모 (작업 5~10개) | 2~3명 |
| 중규모 (10~20개) | 3~5명 |
| 대규모 (20개+) | 5~7명 |

**Phase 전환:** Phase별로 다른 전문가 조합이 필요하면 산출물을 파일로 남기고 다음 Phase의 팀원을 새로 스폰한다. 이전 팀원이 계속 필요하면 `SendMessage`로 컨텍스트를 유지한 채 재활용한다.

## 5. 서브 에이전트

`Agent` 도구로 직접 호출하고 결과만 회수한다. 병렬 실행은 `run_in_background: true` 또는 한 메시지에 여러 호출.

**언제 선택하는가:**
- 단일 전문가 작업(탐색·리뷰·문서 생성)으로 충분할 때
- 팀 통신이 구조적으로 불필요(결과 전달만)하고 팀 오버헤드가 이득보다 클 때

**빌트인 타입 활용:** 읽기 전용 탐색은 `Explore`, 계획 수립은 `Plan`, 실행이 필요하면 `general-purpose`. 커스텀 정의(`.claude/agents/{name}.md`)가 있으면 `subagent_type`으로 지정한다.

## 6. 루프 — 검증자 주도 반복

"실행 → 검증 → 계속/중지 판정"을 종료 조건 충족까지 반복하는 모드. 사람이 매 반복을 확인하는 대신 **검증자(기계적 검증 명령)와 종료 규칙이 판단을 대신한다.** 설계·실행 절차는 `loop` 스킬이 담당한다 — 이 문서는 판별 기준만 요약한다.

**언제 선택하는가:**
- 종료 상태를 기계적으로 판정할 수 있고("테스트 전체 통과", "린트 위반 0건"), 거기까지 반복 수렴이 본질일 때
- 감시-대응: 주기적으로 상태를 확인하고 조건 발생 시 대응할 때

**성립 조건 (loop 스킬이 강제):**
- **자기평가 금지** — 종료 판정은 검증 명령의 종료 코드만. 에이전트의 "다 된 것 같다"는 판정이 아니다.
- **4요소 사용자 확인** — 트리거/실행 단위/검증자/종료 규칙을 사용자와 확정한 뒤에만 실행. 하네스가 임의로 루프를 돌리지 않는다.
- **안전장치 필수** — 최대 반복 + **토큰 예산**(초과 시 자동 중단·보고) + 막힘 판정. 명세는 `docs/loops/{slug}.md`에 기록.

**실행 수단:** `/goal`(조건 충족까지) · `/loop`(주기 반복) · 검증자 게이트(Stop 훅, `hooks-and-permissions.md` §5) · Workflow `while` + `budget.remaining()` 가드(§3). 한 바퀴 내부의 실행은 다른 모드와 조합 가능하다 — 예: 매 반복을 에이전트 팀(생성-검증)으로 돌리고 루프가 종료 조건을 관리.

**에이전트 팀의 생성-검증 루프와의 구분:** 팀 모드의 루프는 팀원 간 협상이 본질이고 종료를 리더가 판단한다. 루프 모드는 종료 판정 자체를 기계화하는 것이 본질이다 — 팀 위에 루프를 씌우면 둘을 겸한다.

## 7. 대량 마이그레이션 — 재시도 루프 파이프라인

대량의 **기계적** 코드 변환(테스트 프레임워크 이관, API 일괄 교체, 린트 규칙 마이그레이션)은 루프와 Workflow의 특수 조합이다. 실무 검증 사례: Airbnb가 Enzyme→RTL 테스트 3,500개를 수작업 추정 18개월 → **6주**로 압축했다(LLM 자동화, >10배). 이 패턴이 통한 이유가 설계 규칙이다:

1. **정교한 프롬프트보다 무식한 재시도 루프가 이긴다.** 파일별로 "변환 → 검증 명령 실행 → 실패면 재시도"를 통과 또는 재시도 상한까지 반복한다. 단순 파일은 ~10회, 롱테일은 50~100회까지 걸렸다 — 재시도 예산을 **파일 난이도에 따라 넉넉히** 잡는다(단일 상한이 아니라).
2. **파일 단위 상태 머신 = per-step 검증 게이트.** 변환을 단계로 쪼개고(예: 프레임워크 리팩터 → 테스트 수정 → 린트 → 타입체크), 각 파일은 **현재 단계의 자동 검증을 통과해야만** 다음 단계로 전진한다. 검증자 게이트(§6, verifierGate)를 파일 단위로 적용한 것이다.
3. **컨텍스트 최소화의 예외 도메인이다.** Airbnb는 프롬프트를 최종 4만~10만 토큰까지 **불렸다** — 관련 파일 50개, 손으로 쓴 few-shot 예시, 같은 프로젝트의 잘 작성된 통과 사례를 퍼부었다. 기계적 변환에서는 "최소 고신호 토큰"(절대 규칙 7)보다 **패턴을 충분히 보여주는 것**이 정확도를 지배한다. 이 예외는 `context-economy.md` §3에도 명시돼 있다.

**실행 모드:** Workflow `pipeline`(파일 = 항목, 단계 = 스테이지) + per-item 재시도. 골격:

```js
// 파일별로 단계를 독립 진행 — A파일이 타입체크 중일 때 B파일은 아직 변환 중일 수 있다
await pipeline(
  args.files,
  (file) => convertWithRetry(file, { maxRetries: 100, check: 'jest' }),  // 재시도 루프 내장
  (file) => convertWithRetry(file, { maxRetries: 30, check: 'lint' }),
  (file) => convertWithRetry(file, { maxRetries: 30, check: 'tsc' }),
)
// convertWithRetry: 검증 통과까지 agent() 재호출, 상한 도달 시 그 파일을 실패로 기록하고 계속
```

**주의:** 검증이 순수 기계적일 때만 이 패턴을 쓴다(§6 자기평가 금지와 동일). 재시도 상한에 도달한 파일은 조용히 버리지 말고 실패 목록으로 산출물에 남긴다 — 롱테일은 사람이 처리한다. 사용자 확인은 루프와 동일하게 선행한다(loop 스킬).

## 8. 하이브리드

Phase마다 특성이 다르면 모드를 섞는다. 오케스트레이터의 각 Phase 상단에 실행 모드를 명시한다 (예: `**실행 모드:** Workflow`).

자주 쓰는 조합:
- **Workflow 수집 → 팀 통합**: 대량 자료를 Workflow로 병렬 수집·검증 → 팀이 토론·합의로 통합
- **팀 생성 → Workflow 검증**: 팀이 기능을 구현 → Workflow가 파일별 검수를 결정적으로 팬아웃
- **서브 정찰 → Workflow 본대**: 서브 에이전트로 작업 목록(work-list)을 먼저 발견 → 목록을 Workflow에 `args`로 전달

## 9. 아키텍처 패턴 → 모드 매핑

| 패턴 | 구조 | 기본 모드 |
|------|------|----------|
| 파이프라인 | A→B→C 순차 의존 | 단계가 고정이면 Workflow `pipeline`, 단계 간 협상이 있으면 팀 |
| 팬아웃/팬인 | 병렬 독립 작업 후 종합 | Workflow (`parallel`/`pipeline`) |
| 전문가 풀 | 상황별 선택 호출 | 서브 에이전트 |
| 생성-검증 | 생성 후 품질 검수 루프 | 재작업 루프가 본질이면 팀, 1회 검증이면 Workflow |
| 감독자 | 중앙이 상태 관리·동적 분배 | 에이전트 팀 (리더 = 감독자) |
| 계층적 위임 | 상위가 하위에 재귀 위임 | 하이브리드 (상위 팀 + 하위 Workflow/서브) |
| 반복 수렴 | 실행→검증→계속/중지, 종료 조건까지 | 루프 (loop 스킬 — 4요소 사용자 확인 + 안전장치) |
| 대량 기계 변환 | 파일 N개 × 단계별 변환+검증 재시도 | 대량 마이그레이션 (§7 — Workflow pipeline + per-item 재시도 예산) |
